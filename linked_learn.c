#include <stdio.h>

struct Node {
    int data;
    struct Node* next;
}

void insertatbegining(struct Node** head, int value) {
    
}

int main() {
    struct Node* head = NULL
    
    return 0;
}